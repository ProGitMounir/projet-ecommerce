import { DhCurrencyPipe } from './dh-currency.pipe';

describe('DhCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new DhCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
